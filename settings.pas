// Avaliable modes:
// * ARRAY_MODE
// * LIST_MODE (actually you can set any caption that differs from ARRAY_MODE
//              or just not define ARRAY_MODE to enable list-based data storage)


{$DEFINE LIST_MODE}
